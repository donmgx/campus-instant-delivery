<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.campus.mapper.PermissionMapper">

    <select id="selectPermissionByRoleId" resultType="String">
        select p.path
        from campus_delivery.sys_role_permission rp
                 join campus_delivery.sys_permission p on rp.permission_id = p.id
        where rp.role_id = #{roleId}
    </select>
</mapper>