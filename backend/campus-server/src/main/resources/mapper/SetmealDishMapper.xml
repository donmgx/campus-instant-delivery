<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.campus.mapper.SetmealDishMapper">

    <select id="selectByDishId" resultType="java.lang.Long">
        select * from campus_delivery.setmeal_dish where dish_id in
        <foreach collection="dishIds" separator="," item="dishId" open="(" close=")">
            #{dishId}
        </foreach>
    </select>


    <insert id="insertBySetmealId">
        insert into campus_delivery.setmeal_dish (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishes" item="setmealDish" separator=",">
            (#{setmealDish.setmealId}, #{setmealDish.dishId}, #{setmealDish.name}, #{setmealDish.price}, #{setmealDish.copies})
        </foreach>
    </insert>


    <delete id="delete">
        delete from campus_delivery.setmeal_dish where setmeal_id in
        <foreach collection="setmealIds" open="(" close=")" separator="," item="setmealId">
            #{setmealId}
        </foreach>
    </delete>

</mapper>