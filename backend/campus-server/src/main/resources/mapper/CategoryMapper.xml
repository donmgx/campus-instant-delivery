<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.campus.mapper.CategoryMapper">
    <update id="update" parameterType="com.campus.entity.Category">
        update campus_delivery.category
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="name != null">name = #{name},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser}</if>
        </set>
        <where>
            id = #{id}
        </where>

    </update>

    <select id="selectPage" resultType="com.campus.entity.Category">
        select * from campus_delivery.category
        <where>
            <if test="name != null and name != ''">
                name like concat('%',#{name},'%')
            </if>
            <if test="type != null and type != ''">
                <if test="name != null and name != ''">and</if>
                type like concat('%',#{type},'%')
            </if>
        </where>
        order by sort
    </select>

    <select id="list" resultType="com.campus.entity.Category">
        select * from campus_delivery.category
        <if test="type != null">where type = #{type}</if>
    </select>


</mapper>